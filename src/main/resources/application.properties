spring.application.name=configs
server.port=8082

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://zautomate-care.cnowg0cmw5vl.eu-north-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Postgres#123
spring.datasource.driver-class-name=org.postgresql.Driver

# Specify the schema to use
spring.jpa.properties.hibernate.default_schema=hms

# Hibernate JPA Configuration (for entity management)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# keycloak configs
keycloak.auth-server-url=http://localhost:8080
keycloak.realm=hms-1
keycloak.resource=hms-client-1
keycloak.client-secret=hcwqUoHkTw6a2p3iOb5u8mVxHhYmXbPe
keycloak.admin-username=admin
keycloak.admin-password=admin

